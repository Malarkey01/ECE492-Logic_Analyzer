# Installation Steps for GUI

1. Install the latest version of Python
2. Type the following command into the terminal: "pip install PySide6" and "pip install PyQt6"
3. Make sure that these are accessible in your environmental path (User)

# Files

1. WinButt.py: Sets a minimum size for a window and adds a button widget to the center of the window.
2. 